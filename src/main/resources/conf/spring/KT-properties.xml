<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- config, appConfig, pagingProperties 기존 설정 -> application.properties 로 이관 -->
    <!--<bean id="config" class="org.jasypt.spring4.xml.encryption.EncryptablePropertiesFactoryBean">
        <property name="encryptor" ref="configurationEncryptor"/>
        <property name="ignoreResourceNotFound" value="true"/>
        <property name="locations">
            <list>
                <value>${jboss.config.common.path:classpath:/conf/env/application.properties}</value>
                <value>
                    ${jboss.config.profile.path:classpath:/conf/env/application-${spring.profiles.active:local}.properties}
                </value>
            </list>
        </property>
        <property name="properties">
            <props>
                <prop key="naverMapKey">YOUR_NAVER_MAP_KEY_HERE</prop>
            </props>
        </property>
    </bean>-->

    <!--<bean id="appConfig" class="org.jasypt.spring4.xml.encryption.EncryptablePropertiesFactoryBean">
        <property name="encryptor" ref="configurationEncryptor"/>
        <property name="ignoreResourceNotFound" value="true"/>
        <property name="locations">
            <list>
                <value>${jboss.config.common.path:classpath:/conf/env/application.properties}</value>
                <value>
                    ${jboss.config.profile.path:classpath:/conf/env/application-${spring.profiles.active:local}.properties}
                </value>
            </list>
        </property>
    </bean>-->

    <!--<bean id="pagingProperties" class="org.jasypt.spring4.xml.encryption.EncryptablePropertiesFactoryBean">
        <property name="encryptor" ref="configurationEncryptor"/>
        <property name="fileEncoding" value="UTF-8"/>
        <property name="locations">
            <list>
                <value>classpath:conf/env/application.properties</value>
            </list>
        </property>
    </bean>-->

</beans>