<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:context="http://www.springframework.org/schema/context"
             xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

    <annotation-driven/>

    <!--
        servlet-context : @Controller 만 스캔
        나머지 (@Service, @Repository, @Component, @Configuration) 는 root-context 에서 로드
    -->
    <context:component-scan base-package="com.kt.ktedu" use-default-filters="false">
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>

    <!-- resource mapping -->
    <resources mapping="/resources/**" location="/resources/">
        <resource-chain resource-cache="false">
            <resolvers>
                <version-resolver>
                    <content-version-strategy patterns="/**"/>
                </version-resolver>
            </resolvers>
        </resource-chain>
    </resources>

    <!-- front library - webjars mapping -->
    <resources mapping="/webjars/**" location="classpath:/META-INF/resources/webjars/"/>

    <beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <beans:property name="prefix" value="/WEB-INF/views/"/>
        <beans:property name="suffix" value=".jsp"/>
        <beans:property name="order" value="1"/>
    </beans:bean>

    <!-- multipart -->
    <beans:bean id="multipartResolver"
                class="org.springframework.web.multipart.support.StandardServletMultipartResolver">
    </beans:bean>

    <!-- direct 접속 허용 (controller 안거치고) -->
    <!--<view-controller path="/directSample" view-name="direct/directSample"/>-->
    <!--<view-controller path="/shareLink.do" view-name="direct/shareLink"/>
    <view-controller path="/pageLink.do" view-name="direct/pageLink"/>
    <view-controller path="/nsso_auth.do" view-name="direct/nsso_auth"/>
    <view-controller path="/nsso_return.do" view-name="direct/nsso_return"/>
    <view-controller path="/sso_logon.do" view-name="direct/sso_logon"/>-->
</beans:beans>
